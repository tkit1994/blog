<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Windows下载关联python</title>
    <url>/2018/05/01/associate-python-file-in-windows.html</url>
    <content><![CDATA[<h1 id="安装anaconda">安装anaconda</h1>
<h2 id="下载anaconda">下载anaconda</h2>
<p>可以从<a href="https://mirrors.tuna.tsinghua.edu.cn/#">清华大学开源镜像站</a>下载, 比从官方下载要快上很多。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ wget https://mirrors.tuna.tsinghua.edu.cn/anaconda/archive/Anaconda3-5.1.0-Windows-x86_64.exe</span><br></pre></td></tr></table></figure>
<p>运行下载的文件完成安装。</p>
<h1 id="设置环境变量">设置环境变量</h1>
<ul>
<li>右键<code>此电脑</code>，选择<code>属性</code>, <code>高级系统设置</code>, <code>环境变量</code>。</li>
<li>新建立如下环境变量<code>CONDA_HOME</code>为anaconda的安装目录。</li>
<li>编辑环境变量<code>path</code>, 加入<code>%CONDA_HOME%\</code>和<code>%CONDA_HOME%\Scripts</code>。</li>
</ul>
<figure>
<img src="/2018/05/01/associate-python-file-in-windows/环境变量.PNG" alt><figcaption>环境变量</figcaption>
</figure>
<figure>
<img src="/2018/05/01/associate-python-file-in-windows/PATH.PNG" alt><figcaption>PATH</figcaption>
</figure>
<h1 id="下载winpython">下载winpython</h1>
<ul>
<li>从github上下载<a href="https://github.com/winpython/winpython">Winpython</a></li>
<li>修改winpython/associate.py</li>
<li>注释<code>unregister(sys.prefix)</code>这一行</li>
<li>运行<code>python -m winpython.associate</code></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> https://github.com/winpython/winpython.git</span><br><span class="line">$ <span class="built_in">cd</span> winpython</span><br><span class="line">$ sed -i <span class="string">&#x27;s/unregister(sys.prefix)/#register(sys.prefix)/g&#x27;</span> winpython/associate.py</span><br><span class="line">$ python -m winpython.associate</span><br></pre></td></tr></table></figure>
<figure>
<img src="/2018/05/01/associate-python-file-in-windows/修改associate.py.PNG" alt><figcaption>修改associate.py</figcaption>
</figure>
<h1 id="修改注册表">修改注册表</h1>
<p>打开注册表编辑器，查找<code>python.File</code>，修改</p>
<p><code>计算机\HKEY_CURRENT_USER\Software\Classes\Python.File\shell\Edit with Spyder\command</code></p>
<p>的值：</p>
<p><code>"D:\ProgramData\Miniconda3\pythonw.exe" "D:\ProgramData\Miniconda3\Scripts\spyder" "%1"</code></p>
<p>为：</p>
<p><code>"D:\ProgramData\Miniconda3\pythonw.exe" "D:\ProgramData\Miniconda3\Scripts\spyder-script.py" "%1"</code></p>
<figure>
<img src="/2018/05/01/associate-python-file-in-windows/修改注册表.PNG" alt><figcaption>修改注册表</figcaption>
</figure>
]]></content>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>编译caffe时使用本地的protobuf</title>
    <url>/2018/06/08/use-local-protobuf-when-compiling-caffe.html</url>
    <content><![CDATA[<h1 id="编译caffe时使用本地的protobuf">编译caffe时使用本地的protobuf</h1>
<h2 id="下载并编译protobuf">下载并编译protobuf</h2>
<p>Protobuf的项目主页可以在<a href="https://github.com/google/protobuf">github</a>上找到。在<a href="https://github.com/google/protobuf/releases">Release页面</a>可以下载对应的代码。下载<a href="https://github.com/google/protobuf/releases/download/v3.5.1/protobuf-cpp-3.5.1.tar.gz">protobuf-cpp-3.5.1.tar.gz</a>，别的版本应该也可以。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ wget https://github.com/google/protobuf/releases/download/v3.5.1/protobuf-cpp-3.5.1.tar.gz</span><br><span class="line"></span><br><span class="line">$ tar -xvf protobuf-cpp-3.5.1.tar.gz</span><br><span class="line"></span><br><span class="line">$ <span class="built_in">cd</span> protobuf-3.5.1</span><br><span class="line"></span><br><span class="line">$ ./configure --prefix=<span class="string">&quot;<span class="variable">$HOME</span>/.local&quot;</span></span><br><span class="line"></span><br><span class="line">$ make -j20</span><br><span class="line"></span><br><span class="line">$ make install</span><br></pre></td></tr></table></figure>
<h1 id="编译caffe">编译caffe</h1>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ unrar x caffe-sal.rar</span><br><span class="line">$ <span class="built_in">cd</span> caffe-sal</span><br><span class="line">$ cp Makefile.config.example Makefile.config</span><br></pre></td></tr></table></figure>
<p>编辑<code>Makefile.config</code></p>
<p>修改 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">INCLUDE_DIRS :&#x3D; $(PYTHON_INCLUDE) &#x2F;usr&#x2F;local&#x2F;include</span><br><span class="line">LIBRARY_DIRS :&#x3D; $(PYTHON_LIB) &#x2F;usr&#x2F;local&#x2F;lib &#x2F;usr&#x2F;lib</span><br></pre></td></tr></table></figure> 为 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">INCLUDE_DIRS :&#x3D; $(PYTHON_INCLUDE) $&#123;HOME&#125;&#x2F;.local&#x2F;include &#x2F;usr&#x2F;local&#x2F;include &#x2F;usr&#x2F;include&#x2F;hdf5&#x2F;serial&#x2F;</span><br><span class="line">LIBRARY_DIRS :&#x3D; $(PYTHON_LIB) $&#123;HOME&#125;&#x2F;.local&#x2F;lib &#x2F;usr&#x2F;local&#x2F;lib &#x2F;usr&#x2F;lib &#x2F;usr&#x2F;lib&#x2F;x86_64-linux-gnu&#x2F;hdf5&#x2F;serial&#x2F;</span><br></pre></td></tr></table></figure> 设置<code>PATH</code>环境变量，因为要使用刚才编译好的<code>protoc</code>,然后进行编译,j后面的数字是要使用的线程数，一般为cpu的线程数+1,只影响编译的速度 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ export PATH&#x3D;$&#123;HOME&#125;&#x2F;.local&#x2F;bin:$PATH</span><br><span class="line">$ which protoc</span><br><span class="line">&#x2F;home&#x2F;ncepu&#x2F;.local&#x2F;bin&#x2F;protoc</span><br><span class="line">$ make -j41</span><br></pre></td></tr></table></figure> 每次运行程序前设置环境变量<code>LD_LIBRARY_PATH</code>,或者把它加到.bashrc里面去。 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">export LD_LIBRARY_PATH&#x3D;$&#123;HOME&#125;&#x2F;.local&#x2F;lib:$LD_LIBRARY_PATH</span><br></pre></td></tr></table></figure></p>
<h2 id="查看链接库的版本">查看链接库的版本</h2>
<p>用下面的命令查看caffe链接的库的版本，可以看到是刚才编译好的版本。 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ ldd build&#x2F;tools&#x2F;caffe | grep protobuf</span><br><span class="line">libprotobuf.so.15 &#x3D;&gt; &#x2F;home&#x2F;ncepu&#x2F;.local&#x2F;lib&#x2F;libprotobuf.so.15 (0x00007f7795fc1000)</span><br></pre></td></tr></table></figure></p>
]]></content>
  </entry>
  <entry>
    <title>using latex in matplotlib</title>
    <url>/2019/09/12/using-latex-in-matplotlib.html</url>
    <content><![CDATA[<h1 id="在matplotlib中使用latex渲染">在matplotlib中使用latex渲染</h1>
<h2 id="代码">代码</h2>
<p>在代码中加入如下的代码就可以在matplotlib中使用latex渲染文字 <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib</span><br><span class="line">matplotlib.use(<span class="string">&quot;pgf&quot;</span>)</span><br><span class="line">pgf_with_custom_preamble = &#123;</span><br><span class="line">    <span class="comment"># &quot;font.size&quot;: 40,</span></span><br><span class="line">    <span class="string">&quot;pgf.rcfonts&quot;</span>: <span class="literal">False</span>,</span><br><span class="line">    <span class="string">&quot;text.usetex&quot;</span>: <span class="literal">True</span>,</span><br><span class="line">    <span class="string">&quot;pgf.preamble&quot;</span>: [</span><br><span class="line">        <span class="comment"># math setup:</span></span><br><span class="line">        <span class="string">r&quot;\usepackage&#123;unicode-math&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\usepackage[utf8]&#123;inputenc&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\usepackage&#123;xeCJK&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setmainfont&#123;Times New Roman&#125; % Set main font for ASCII&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setsansfont&#123;Times New Roman&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setmonofont&#123;Times New Roman&#125;&quot;</span></span><br><span class="line">        <span class="string">r&quot;\setCJKmainfont&#123;SimSun&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setCJKsansfont&#123;SimSun&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setCJKmonofont&#123;SimSun&#125;&quot;</span></span><br><span class="line">    ],</span><br><span class="line">    <span class="string">&quot;pdf.fonttype&quot;</span>: <span class="number">42</span>,</span><br><span class="line">&#125;</span><br><span class="line">matplotlib.rcParams.update(pgf_with_custom_preamble)</span><br></pre></td></tr></table></figure></p>
<p>完整的代码如下： <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib</span><br><span class="line">matplotlib.use(<span class="string">&quot;pgf&quot;</span>)</span><br><span class="line">pgf_with_custom_preamble = &#123;</span><br><span class="line">    <span class="comment"># &quot;font.size&quot;: 40,</span></span><br><span class="line">    <span class="string">&quot;pgf.rcfonts&quot;</span>: <span class="literal">False</span>,</span><br><span class="line">    <span class="string">&quot;text.usetex&quot;</span>: <span class="literal">True</span>,</span><br><span class="line">    <span class="string">&quot;pgf.preamble&quot;</span>: [</span><br><span class="line">        <span class="comment"># math setup:</span></span><br><span class="line">        <span class="string">r&quot;\usepackage&#123;unicode-math&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\usepackage[utf8]&#123;inputenc&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\usepackage&#123;xeCJK&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setmainfont&#123;Times New Roman&#125; % Set main font for ASCII&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setsansfont&#123;Times New Roman&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setmonofont&#123;Times New Roman&#125;&quot;</span></span><br><span class="line">        <span class="string">r&quot;\setCJKmainfont&#123;SimSun&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setCJKsansfont&#123;SimSun&#125;&quot;</span>,</span><br><span class="line">        <span class="string">r&quot;\setCJKmonofont&#123;SimSun&#125;&quot;</span></span><br><span class="line">    ],</span><br><span class="line">    <span class="string">&quot;pdf.fonttype&quot;</span>: <span class="number">42</span>,</span><br><span class="line">&#125;</span><br><span class="line">matplotlib.rcParams.update(pgf_with_custom_preamble)</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> seaborn <span class="keyword">as</span> sns</span><br><span class="line"></span><br><span class="line">sns.<span class="built_in">set</span>()</span><br><span class="line">df = pd.read_hdf(<span class="string">&#x27;./label.h5&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 性别分布</span></span><br><span class="line">gender = df[<span class="string">&#x27;gender&#x27;</span>]</span><br><span class="line"></span><br><span class="line">gender = gender.fillna(<span class="string">&#x27;unkonwn&#x27;</span>)</span><br><span class="line">counts = pd.value_counts(gender)</span><br><span class="line">plt.figure(figsize=(<span class="number">10</span>, <span class="number">8</span>))</span><br><span class="line">counts.plot.pie(legend=<span class="literal">True</span>, autopct=<span class="keyword">lambda</span> p: <span class="string">&#x27;&#123;:.2f&#125;%(&#123;:.0f&#125;)&#x27;</span>.<span class="built_in">format</span>(</span><br><span class="line">    p, (p/<span class="number">100</span>)*counts.<span class="built_in">sum</span>()), title=<span class="string">&quot;Gender distribution&quot;</span>)</span><br><span class="line">plt.savefig(<span class="string">&#x27;./figures/gender.png&#x27;</span>)</span><br><span class="line">plt.savefig(<span class="string">&#x27;./figures/gender&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># # 年龄分布</span></span><br><span class="line"></span><br><span class="line">age = df[<span class="string">&#x27;age&#x27;</span>]</span><br><span class="line">plt.clf()</span><br><span class="line">age = age.dropna()</span><br><span class="line">sns.distplot(age)</span><br><span class="line">plt.title(<span class="string">&#x27;kde plot&#x27;</span>)</span><br><span class="line">plt.savefig(<span class="string">&#x27;./figures/age.png&#x27;</span>)</span><br><span class="line">plt.savefig(<span class="string">&#x27;./figures/age&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># # 诊断分布</span></span><br><span class="line"></span><br><span class="line">tmp = df.iloc[:, <span class="number">2</span>:]</span><br><span class="line">plt.clf()</span><br><span class="line">counts = tmp.<span class="built_in">sum</span>() / tmp.shape[<span class="number">0</span>]</span><br><span class="line">counts.plot(kind=<span class="string">&#x27;bar&#x27;</span>, title=<span class="string">&#x27;Percentage&#x27;</span>)</span><br><span class="line">plt.tight_layout()</span><br><span class="line">plt.savefig(<span class="string">&#x27;./figures/diagnose.png&#x27;</span>)</span><br><span class="line"></span><br><span class="line">plt.savefig(<span class="string">&#x27;./figures/diagnose&#x27;</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure></p>
<p>结果如图：</p>
<figure>
<img src="/2019/09/12/using-latex-in-matplotlib/gender.svg" alt><figcaption>gender</figcaption>
</figure>
<figure>
<img src="/2019/09/12/using-latex-in-matplotlib/age.svg" alt><figcaption>age</figcaption>
</figure>
<figure>
<img src="/2019/09/12/using-latex-in-matplotlib/diagnose.svg" alt><figcaption>dianose</figcaption>
</figure>
]]></content>
      <tags>
        <tag>python</tag>
        <tag>matplotlib</tag>
        <tag>latex</tag>
      </tags>
  </entry>
  <entry>
    <title>使用docker搭建深度学习环境</title>
    <url>/2018/06/10/%E4%BD%BF%E7%94%A8docker%E6%90%AD%E5%BB%BA%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E7%8E%AF%E5%A2%83.html</url>
    <content><![CDATA[<h1 id="docker">Docker</h1>
<p>Docker是一种虚拟化容器，可能把运行环境与操作系统隔离起来，使开发环境与操作系统分离，方便多个开发环境的管理。与虚拟机相比，它更加轻量，方便。</p>
<h1 id="系统环境">系统环境</h1>
<ul>
<li>Ubuntu 16.04 LTS</li>
<li>Nvidia GTX Titan Xp(其它Nvidia的显卡应该也可以)</li>
</ul>
<h1 id="安装nvidia驱动已经安装过的可以跳过">安装Nvidia驱动（已经安装过的可以跳过）</h1>
<p>可以参照这篇<a href="https://websiteforstudents.com/install-proprietary-nvidia-gpu-drivers-on-ubuntu-16-04-17-10-18-04/">文章</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ sudo add-apt-repository ppa:graphics-drivers/ppa</span><br><span class="line">$ sudo apt update</span><br><span class="line">$ sudo apt install nvidia-387</span><br></pre></td></tr></table></figure>
<h1 id="安装docker-ce和nvidia-docker">安装Docker-ce和Nvidia-Docker</h1>
<h2 id="安装docker-ce">安装Docker-ce</h2>
<p>docker-ce的安装可以参照这里的<a href="https://docs.docker.com/install/linux/docker-ce/ubuntu/#uninstall-old-versions">官方文档</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 卸载Ubuntu官方源中的docker.io（如果之前安装了的话）</span></span><br><span class="line">$ sudo apt-get remove docker docker-engine docker.io</span><br><span class="line"><span class="comment"># 更新apt package index.</span></span><br><span class="line">$ sudo apt-get update</span><br><span class="line"><span class="comment"># 安装一些要用到了一些包</span></span><br><span class="line">$ sudo apt-get install \</span><br><span class="line">    apt-transport-https \</span><br><span class="line">    ca-certificates \</span><br><span class="line">    curl \</span><br><span class="line">    software-properties-common</span><br><span class="line"><span class="comment"># 添加docker官方的GPG KEY</span></span><br><span class="line">$ curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -</span><br><span class="line"><span class="comment"># 添加docker官方的源</span></span><br><span class="line">$ sudo add-apt-repository \</span><br><span class="line">   <span class="string">&quot;deb [arch=amd64] https://download.docker.com/linux/ubuntu \</span></span><br><span class="line"><span class="string">   <span class="subst">$(lsb_release -cs)</span> \</span></span><br><span class="line"><span class="string">   stable&quot;</span></span><br><span class="line"><span class="comment"># 更新apt package index.</span></span><br><span class="line">$ sudo apt-get update</span><br><span class="line"><span class="comment"># 安装docker-ce</span></span><br><span class="line">$ sudo apt-get install docker-ce</span><br></pre></td></tr></table></figure>
<h2 id="安装nvidia-docker">安装Nvidia-docker</h2>
<p>在docker-ce中并不能使用GPU,要安装nvidia-docker才可以，nvidia-docker的安装可以参照其<a href="https://github.com/nvidia/nvidia-docker/wiki/Installation-(version-2.0)">官方文档</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 配置软件源</span></span><br><span class="line">$ curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | \</span><br><span class="line">  sudo apt-key add -</span><br><span class="line">$ distribution=$(. /etc/os-release;<span class="built_in">echo</span> $ID<span class="variable">$VERSION_ID</span>)</span><br><span class="line">$ curl -s -L https://nvidia.github.io/nvidia-docker/<span class="variable">$distribution</span>/nvidia-docker.list | \</span><br><span class="line">  sudo tee /etc/apt/sources.list.d/nvidia-docker.list</span><br><span class="line">sudo apt-get update</span><br><span class="line"><span class="comment"># 安装Nvidia-docker</span></span><br><span class="line">$ sudo apt-get install nvidia-docker2</span><br></pre></td></tr></table></figure>
<h2 id="配置docker">配置Docker</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启动Docker服务</span></span><br><span class="line">sudo service docker start</span><br><span class="line"><span class="comment"># 将当前用户加入docker group</span></span><br><span class="line">sudo gpasswd -a <span class="variable">$USER</span> docker</span><br></pre></td></tr></table></figure>
<h2 id="优化dockerhub连接可选">优化Dockerhub连接(可选)</h2>
<p><a href="https://hub.docker.com/">Dockerhub</a>是一个托管docker镜像的地方，就像<a href="https://github.com/">Github</a>是一个托管代码的地方一样。上面有大量别人已经配置好的docker环境可以直接使用，十分方便。但是在国内访问十分缓慢。</p>
<p>可以参照这个<a href="https://blog.csdn.net/evandeng2009/article/details/53893789">博客</a>使用阿里云容器hub对Dockerhub进行加速，阿里云容器hub的加速是免费的，实测在学校能达到大概1M/s，而直接连接Dockerhub的速度则惨不忍睹。</p>
<figure>
<img src="/2018/06/10/%E4%BD%BF%E7%94%A8docker%E6%90%AD%E5%BB%BA%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E7%8E%AF%E5%A2%83/阿里云.PNG" alt><figcaption>阿里云容器hub</figcaption>
</figure>
<p>复制上面的专属加速地层地址后编辑<code>/etc/docker//etc/docker/daemon.json</code> 在后面加入如下一行(注意json的格式，要写在<code>&#123;&#125;</code>里面，上一行结尾要有<code>,</code>)：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">&quot;registry-mirrosrs&quot;:[&quot;刚才复制的地址&quot;]</span><br></pre></td></tr></table></figure>
<figure>
<img src="/2018/06/10/%E4%BD%BF%E7%94%A8docker%E6%90%AD%E5%BB%BA%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E7%8E%AF%E5%A2%83/修改daemon.json.PNG" alt><figcaption>修改daemon.json</figcaption>
</figure>
<p>至此Docker和Nvidia环境安装配置完成</p>
<h1 id="使用docker">使用docker</h1>
<h2 id="简单使用">简单使用</h2>
<p>一些常用的镜像可以直接在dockerhub上搜索，然后通过dcoker运行。比如下面的这个<a href="https://hub.docker.com/r/tensorflow/tensorflow/">Tensorflow</a>, 通过下面的这个命令即可开箱及用。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ nvidia-docker run -it -p 8888:8888 tensorflow/tensorflow:latest-gpu</span><br></pre></td></tr></table></figure>
<h2 id="使用nvidia提供的cudacudnn环境镜像">使用Nvidia提供的cuda&amp;cudnn环境镜像</h2>
<p>如果在电脑上安装多个版本的<code>cuda</code>、<code>cudnn</code>等库并且管理其版本是一件很麻烦的事情。有了docker之后可以使用Nvidia官方提供的docker镜像，在这些镜像中cuda和cudnn都是已经安装配置好了的。如果dockerhub上面没有我们想要的一些环境的话，可以在这个环境的基础上继续构建我们的环境。</p>
<p>以<code>8.0-cudnn5-devel-ubuntu16.04</code>为例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$  nvidia-docker run --name <span class="built_in">test</span> -it nvidia/cuda:8.0-cudnn5-devel-ubuntu16.04 bash</span><br></pre></td></tr></table></figure>
<p>如下图片所示可以进入对应的环境，然后可以像正常使用Linux一样继续构建环境。</p>
<figure>
<img src="/2018/06/10/%E4%BD%BF%E7%94%A8docker%E6%90%AD%E5%BB%BA%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E7%8E%AF%E5%A2%83/8.0-cudnn5-devel-ubuntu16.04.PNG" alt><figcaption>8.0-cudnn5-devel-ubuntu16.04</figcaption>
</figure>
<p>要退出环境的话，输入<code>exit</code>或者使用快捷键<code>ctrl=d</code>就可以了。</p>
<p>配置好环境退出后后使用可以使用<code>docker commit</code>将<code>container</code>保存成<code>image</code>从而使环境持久化，方便生成同样的环境。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker commit <span class="built_in">test</span> <span class="built_in">test</span>:<span class="built_in">test</span></span><br><span class="line">sha256:e9338a3b4aede076ab566c44b1127f5c1b18fd3dcb7cb3e02e19856f21db0c5b</span><br></pre></td></tr></table></figure>
<p>可以通过下面的命令新建一个同样的环境</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ nvidia-docker run -it --name test01 <span class="built_in">test</span>:<span class="built_in">test</span> bash</span><br></pre></td></tr></table></figure>
<p>如果只是想运行刚才的<code>container</code>的话，可以用<code>docker container start</code>命令。</p>
<p>更加详细的使用方法可以参照Docker的<a href="https://docs.docker.com/">官方文档</a>和一些中文教程。</p>
<h2 id="使用dockerfile">使用Dockerfile</h2>
<p>Dockerfile可以用来自动化构建Docker Image.</p>
<h3 id="构建caffe-sal">构建caffe-sal</h3>
<p>参考caffe官方的<a href="https://github.com/BVLC/caffe/tree/master/docker">Dockerfile</a>编写自己的Docerfile</p>
<p>目录结构 <img src="/2018/06/10/%E4%BD%BF%E7%94%A8docker%E6%90%AD%E5%BB%BA%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E7%8E%AF%E5%A2%83/目录结构.PNG" alt="目结构"></p>
<p>Dockerfile的内容</p>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">FROM</span> nvidia/cuda</span><br><span class="line"></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> apt-get update &amp;&amp; apt-get install -y --no-install-recommends \</span></span><br><span class="line"><span class="bash">	cpio \</span></span><br><span class="line"><span class="bash">        build-essential \</span></span><br><span class="line"><span class="bash">        git \</span></span><br><span class="line"><span class="bash">        wget \</span></span><br><span class="line"><span class="bash">        numactl \</span></span><br><span class="line"><span class="bash">        vim \</span></span><br><span class="line"><span class="bash">        libopenblas-dev\</span></span><br><span class="line"><span class="bash">        screen \</span></span><br><span class="line"><span class="bash">        libmlx4-1 libmlx5-1 ibutils  rdmacm-utils libibverbs1 ibverbs-utils perftest infiniband-diags \</span></span><br><span class="line"><span class="bash">        openmpi-bin libopenmpi-dev \</span></span><br><span class="line"><span class="bash">        libboost-all-dev \</span></span><br><span class="line"><span class="bash">        libgflags-dev \</span></span><br><span class="line"><span class="bash">        libgoogle-glog-dev \</span></span><br><span class="line"><span class="bash">        libhdf5-serial-dev \</span></span><br><span class="line"><span class="bash">        libleveldb-dev \</span></span><br><span class="line"><span class="bash">        liblmdb-dev \</span></span><br><span class="line"><span class="bash">        libopencv-dev \</span></span><br><span class="line"><span class="bash">        libprotobuf-dev \</span></span><br><span class="line"><span class="bash">        libsnappy-dev \</span></span><br><span class="line"><span class="bash">        protobuf-compiler</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ENV</span> CAFFE_ROOT=/opt/caffe</span><br><span class="line"><span class="keyword">WORKDIR</span><span class="bash"> <span class="variable">$CAFFE_ROOT</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> caffe-sal caffe-sal</span></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="bash"> <span class="variable">$CAFFE_ROOT</span>/caffe-sal</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> Makefile.config Makefile.config</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> make -j40</span></span><br></pre></td></tr></table></figure>
<p>Makefile.config的内容</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">## Refer to http://caffe.berkeleyvision.org/installation.html</span></span><br><span class="line"><span class="comment"># Contributions simplifying and improving our build system are welcome!</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># cuDNN acceleration switch (uncomment to build with cuDNN).</span></span><br><span class="line"><span class="comment"># USE_CUDNN := 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># CPU-only switch (uncomment to build without GPU support).</span></span><br><span class="line"><span class="comment"># CPU_ONLY := 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># To customize your choice of compiler, uncomment and set the following.</span></span><br><span class="line"><span class="comment"># N.B. the default for Linux is g++ and the default for OSX is clang++</span></span><br><span class="line"><span class="comment"># CUSTOM_CXX := g++</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># CUDA directory contains bin/ and lib/ directories that we need.</span></span><br><span class="line">CUDA_DIR := /usr/local/cuda</span><br><span class="line"><span class="comment"># On Ubuntu 14.04, if cuda tools are installed via</span></span><br><span class="line"><span class="comment"># &quot;sudo apt-get install nvidia-cuda-toolkit&quot; then use this instead:</span></span><br><span class="line"><span class="comment"># CUDA_DIR := /usr</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># CUDA architecture setting: going with all of them.</span></span><br><span class="line"><span class="comment"># For CUDA &lt; 6.0, comment the *_50 lines for compatibility.</span></span><br><span class="line">CUDA_ARCH := -gencode arch=compute_30,code=sm_30 \</span><br><span class="line">		-gencode arch=compute_35,code=sm_35 \</span><br><span class="line">		-gencode arch=compute_50,code=sm_50 \</span><br><span class="line">		-gencode arch=compute_50,code=compute_50</span><br><span class="line"></span><br><span class="line"><span class="comment"># BLAS choice:</span></span><br><span class="line"><span class="comment"># atlas for ATLAS (default)</span></span><br><span class="line"><span class="comment"># mkl for MKL</span></span><br><span class="line"><span class="comment"># open for OpenBlas</span></span><br><span class="line">BLAS := open</span><br><span class="line"><span class="comment"># Custom (MKL/ATLAS/OpenBLAS) include and lib directories.</span></span><br><span class="line"><span class="comment"># Leave commented to accept the defaults for your choice of BLAS</span></span><br><span class="line"><span class="comment"># (which should work)!</span></span><br><span class="line"><span class="comment"># BLAS_INCLUDE := /path/to/your/blas</span></span><br><span class="line"><span class="comment"># BLAS_LIB := /path/to/your/blas</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Homebrew puts openblas in a directory that is not on the standard search path</span></span><br><span class="line"><span class="comment"># BLAS_INCLUDE := $(shell brew --prefix openblas)/include</span></span><br><span class="line"><span class="comment"># BLAS_LIB := $(shell brew --prefix openblas)/lib</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># This is required only if you will compile the matlab interface.</span></span><br><span class="line"><span class="comment"># MATLAB directory should contain the mex binary in /bin.</span></span><br><span class="line"><span class="comment"># MATLAB_DIR := /usr/local</span></span><br><span class="line"><span class="comment"># MATLAB_DIR := /Applications/MATLAB_R2012b.app</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># <span class="doctag">NOTE:</span> this is required only if you will compile the python interface.</span></span><br><span class="line"><span class="comment"># We need to be able to find Python.h and numpy/arrayobject.h.</span></span><br><span class="line">PYTHON_INCLUDE := /usr/<span class="keyword">include</span>/python2.7 \</span><br><span class="line">		/usr/lib/python2.7/dist-packages/numpy/core/<span class="keyword">include</span></span><br><span class="line"><span class="comment"># Anaconda Python distribution is quite popular. Include path:</span></span><br><span class="line"><span class="comment"># Verify anaconda location, sometimes it&#x27;s in root.</span></span><br><span class="line"><span class="comment"># ANACONDA_HOME := $(HOME)/anaconda</span></span><br><span class="line"><span class="comment"># PYTHON_INCLUDE := $(ANACONDA_HOME)/include \</span></span><br><span class="line">		<span class="comment"># $(ANACONDA_HOME)/include/python2.7 \</span></span><br><span class="line">		<span class="comment"># $(ANACONDA_HOME)/lib/python2.7/site-packages/numpy/core/include \</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># We need to be able to find libpythonX.X.so or .dylib.</span></span><br><span class="line">PYTHON_LIB := /usr/lib</span><br><span class="line"><span class="comment"># PYTHON_LIB := $(ANACONDA_HOME)/lib</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Homebrew installs numpy in a non standard path (keg only)</span></span><br><span class="line"><span class="comment"># PYTHON_INCLUDE += $(dir $(shell python -c &#x27;import numpy.core; print(numpy.core.__file__)&#x27;))/include</span></span><br><span class="line"><span class="comment"># PYTHON_LIB += $(shell brew --prefix numpy)/lib</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Uncomment to support layers written in Python (will link against Python libs)</span></span><br><span class="line"><span class="comment"># WITH_PYTHON_LAYER := 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Whatever else you find you need goes here.</span></span><br><span class="line">INCLUDE_DIRS := <span class="variable">$(PYTHON_INCLUDE)</span> $&#123;HOME&#125;/.local/<span class="keyword">include</span> /usr/local/<span class="keyword">include</span> /usr/<span class="keyword">include</span>/hdf5/serial/</span><br><span class="line">LIBRARY_DIRS := <span class="variable">$(PYTHON_LIB)</span> $&#123;HOME&#125;/.local/lib /usr/local/lib /usr/lib /usr/lib/x86_64-linux-gnu/hdf5/serial/</span><br><span class="line"></span><br><span class="line"><span class="comment"># If Homebrew is installed at a non standard location (for example your home directory) and you use it for general dependencies</span></span><br><span class="line"><span class="comment"># INCLUDE_DIRS += $(shell brew --prefix)/include</span></span><br><span class="line"><span class="comment"># LIBRARY_DIRS += $(shell brew --prefix)/lib</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Uncomment to use `pkg-config` to specify OpenCV library paths.</span></span><br><span class="line"><span class="comment"># (Usually not necessary -- OpenCV libraries are normally installed in one of the above $LIBRARY_DIRS.)</span></span><br><span class="line"><span class="comment"># USE_PKG_CONFIG := 1</span></span><br><span class="line"></span><br><span class="line">BUILD_DIR := build</span><br><span class="line">DISTRIBUTE_DIR := distribute</span><br><span class="line"></span><br><span class="line"><span class="comment"># Uncomment for debugging. Does not work on OSX due to https://github.com/BVLC/caffe/issues/171</span></span><br><span class="line"><span class="comment"># DEBUG := 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The ID of the GPU that &#x27;make runtest&#x27; will use to run unit tests.</span></span><br><span class="line">TEST_GPUID := 0</span><br><span class="line"></span><br><span class="line"><span class="comment"># enable pretty build (comment to see full commands)</span></span><br><span class="line">Q ?= @</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 运行构建命令</span></span><br><span class="line">$ docker build --rm -t caffe-sal:latest .</span><br></pre></td></tr></table></figure>
<p>构建结果： <img src="/2018/06/10/%E4%BD%BF%E7%94%A8docker%E6%90%AD%E5%BB%BA%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E7%8E%AF%E5%A2%83/构建结果.PNG" alt="构建结果"></p>
<p>运行构建好的<code>Image</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ nvidia-docker run it caffe-sal:latest bash</span><br></pre></td></tr></table></figure>
<p>运行结果 <img src="/2018/06/10/%E4%BD%BF%E7%94%A8docker%E6%90%AD%E5%BB%BA%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E7%8E%AF%E5%A2%83/运行结果.PNG" alt="运行结果"></p>
]]></content>
      <tags>
        <tag>docker</tag>
        <tag>深度学习</tag>
        <tag>环境搭建</tag>
      </tags>
  </entry>
</search>
